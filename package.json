{
  "name": "dicom-healthimaging-poc",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "eslint . && terraform fmt -check terraform/",
    "fix": "eslint . --fix && terraform fmt terraform/",
    "install:all": "cd frontend && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "eslint": "^9.31.0",
    "@eslint/js": "^9.31.0"
  }
}
