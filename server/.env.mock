# Mock/Development Environment Configuration
NODE_ENV=development
CONFIG_PROFILE=mock

# Server Configuration
PORT=3000

# AWS Configuration (not used in mock mode, but required for config structure)
AWS_REGION=us-east-1

# DICOM Configuration (mock values)
HEALTHIMAGING_DATASTORE_ID=mock-datastore-12345

# CORS Configuration
CORS_ORIGIN=http://localhost:3001